'use strict';

describe('Dummy: Test', function() {

    // load the controller's module
    beforeEach(module('oxChartSamples'));
 
    it('should pass', function() {
        expect(3).toBe(3);
    });
});